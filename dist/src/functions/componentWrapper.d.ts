import { ComponentWrapperProps } from "../types";
import React from "react";
declare const ComponentWrapper: ({ children, type, ...props }: ComponentWrapperProps) => React.JSX.Element;
export default ComponentWrapper;
